<%- include('partials/header') %>




<<table class="table table-striped table-hover">
      <thead>
  
  <tr>
    <th>Num</th>
    <th>ชื่อผู้ใช้งาน</th>
    <th>นามสกุล</th>
    <th>Username</th>
    <th>สถานะ</th>
    <th>บทบาทหน้าที่</th>
    <th>คณะ</th>
    <th>ชั้นปี</th>
  </tr>
  </thead>
      <% for(var i=0; i<title.length; i++) {%>
  <tbody>
    <tr action="/home">
      <td type="1">1</td>
      <td><%= title[i].firstName %></td>
      <td><%= title[i].lastName %></td>
      <td><%= title[i].username %></td>
      <td><%= title[i].Status %></td>
      <td><%= title[i].typeUser %></td>
      <td><%= title[i].faculty %></td>
      <td><%= title[i].year %></td>


    </tr>
      </tbody>
       <% } %>

    </table> 
<%- include('partials/footer') %>